#include <stdio.h>
#include <stdlib.h>

/**
 * main - main function with arguments
 * @argc: checks integer value of elements in array argv
 * @argv: array of string inputs
 * Return: number of elements
 */
int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
